<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "" %> -- Scrumwich</title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <nav class="top-bar" data-topbar>
    <ul class="title-area">
      <li class="name">
        <h1><%= link_to "Today", root_url %></h1>
      </li>
    </ul>

    <section class="top-bar-section">
      <ul class="right">
        <li class="has-dropdown">
          <a href="">Projects</a>
          <ul class="dropdown">
            <% if @user %>
              <% @user.projects.each do |proj| %>
                <li><%= link_to proj.name, proj %></li>
              <% end %>
            <% end %>
          </ul>
        </li>
        <li class="active">
          <% if current_user %>
            <%= link_to "Add Today's Entry", new_entry_path %>
          <% end %>
        </li>
      </ul>

      <%#<ul class="left">%>

      <%#</ul>%>
    </section>
  </nav>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
